# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: {{PROJECT_SLUG}}
metadata:
  template: mcp-auth-scaffold@0.1.0
services:
  api:
    project: .
    language: ts
    host: function
    remoteBuild: true
